to access the vm's go to 

google gcp and log in 
access the vm via the commute engine

to run do the following in terminal

vagrant init 
vagrant plan
vagrant apply 

if you have a server check rules to access it via the web
go to vpc network and add rule
then edit instance to follow the rule

when working with variable you can pass data to them as following 
terraform apply -var "name=jenkins"
with variables you can create a folder that passes the same data and then run the following 
terraform apply -var-file=variables/jenkins.tfvar
jenkins can be named anything 
it is best practice to put these files in a seperate folder
